{% extends 'base.html.twig' %}

{% block title %}Strowberry{% endblock %}

{% block body %}
    <h1>Strowberry</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ strowberry.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ strowberry.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('strowberry_index') }}">back to list</a>

    <a href="{{ path('strowberry_edit', {'id': strowberry.id}) }}">edit</a>

    {{ include('strowberry/_delete_form.html.twig') }}
{% endblock %}
