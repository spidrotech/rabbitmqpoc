{% extends 'base.html.twig' %}

{% block title %}Strowberry index{% endblock %}

{% block body %}
    <h1>Strowberry index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for strowberry in strowberries %}
            <tr>
                <td>{{ strowberry.id }}</td>
                <td>{{ strowberry.name }}</td>
                <td>
                    <a href="{{ path('strowberry_show', {'id': strowberry.id}) }}">show</a>
                    <a href="{{ path('strowberry_edit', {'id': strowberry.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('strowberry_new') }}">Create new</a>
{% endblock %}
